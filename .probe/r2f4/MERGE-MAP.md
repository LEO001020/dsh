# R2-F4 merge map: exactly what this writer changed, for a three-way merge

Three writers touched `packages/dsh-daily-work/src/data-service.ts` (R2-F4 = this
slice, R6 = `G-SEAM-64` artifact-root, R7 = `page`/`walk` store-realm cursor). This
file lists the functions and regions R2-F4 touched so the merge is mechanical.

## packages/dsh-daily-work/src/data-service.ts

| Region | R2-F4 change |
|---|---|
| import block | `LocalArtifactStore` -> `AttachmentArtifactStore` in the `./artifacts.ts` import list. Nothing else in the block. |
| `class DataPlaneService` field `readonly store` | type `LocalArtifactStore` -> `AttachmentArtifactStore`. |
| `class DataPlaneService` constructor body | the `new LocalArtifactStore(root, {quotaBytes})` call -> `new AttachmentArtifactStore(ctx.attachments, root, {quotaBytes})`, plus a comment block explaining why the bytes come from the capability. **The root expression itself (`config.artifactRoot ?? defaultArtifactRoot(ctx)`) is UNCHANGED** -- R6 owns that. |

NOT touched by R2-F4 in this file: `defaultArtifactRoot` (R6), `page` (R7),
`walk` (R7), `capture`, `lineIndex`, `readLine`, `readRange`, `join`, `project`,
`resolve`, `referenceOf`, `reconcile`, `refuseForgedClaims`, `parseObservation`,
`StorageReferenceLog` (whole class), `open`, `close`, `grantRevision`,
`revokeAndRebump`, `requireLog`, `checkpoint`, all schemas and constants.

## packages/dsh-daily-work/src/data-plugin.ts

| Region | R2-F4 change |
|---|---|
| `export const inject` | `['storageDomain']` -> `['storageDomain', 'attachments']`, plus the comment explaining that `attachments` is a hard readiness gate for the byte store. |

NOT touched: `name`, `Config`, `apply`.

## packages/dsh-daily-work/src/artifacts.ts

| Region | R2-F4 change |
|---|---|
| module doc comment | rewritten: the F4 defect, why the deep import was wrong, what replaced it, and the per-arm integrity statement. |
| import block | `publishImmutableObjectStream` from `@deepseek-ai/dsh-attachment-local/src/store.ts` -> `AttachmentId`, `type AttachmentStore`, `type FileAttachmentRef` from `@deepseek-ai/dsh-attachment`. `node:fs/promises` import narrowed (`stat`/`unlink` dropped). |
| `interface ArtifactStore` | doc comment only; the six methods are unchanged. |
| `interface ArtifactIndexEntry` | **NEW** -- the durable index record (`artifact`, `sha256`, `name`, `bytes`, `publishedAt`). |
| `class LocalArtifactStore` | **RENAMED** to `AttachmentArtifactStore`, and its body rewritten: `put`/`stat`/`openRange`/`verify`/`remove`/`collectGarbage` now go through the capability and the index; `loadEntry`, `saveEntry`, `refOf`, `hostPath`, `streamRange`, `provider`, `indexRoot`, `indexPathOf` are **NEW** private/public members. `isPinned`, `setPinned`, `tombstoneOf` are unchanged in behaviour. |
| `digestOfProviderRef` | **NEW** exported helper crossing the provider-ref and artifact-ref namespaces. |
| `findArtifactError` | doc comment only (no longer names the private module). |
| everything from `artifactRefOf` onward | **UNCHANGED** except `reconcileStore`, whose `store` parameter type changed to `AttachmentArtifactStore` and whose directory walk now reads the `index/` tree instead of `objects/`. |

## packages/dsh-daily-work/src/data-plane.test.ts

`makePlane` and the new `makeStore` helper now mount `AttachmentLocal` and build an
`AttachmentArtifactStore`; `mountService` mounts the provider; the SIGKILL child
script mounts it too; the DATA-08 tamper tests resolve the object path through
`store.hostPath(...)`; the artifact-root assertion reads the index root.
`LocalArtifactStore` -> `AttachmentArtifactStore` throughout.

## packages/dsh-daily-work/src/upg-gates.test.ts

Two deep imports of `@deepseek-ai/dsh-attachment-local/src/store.ts` replaced by the
public capability; the GC test mounts the provider and walks the `index/` tree.

## packages/dsh-daily-work/src/no-src-imports.test.ts

**NEW FILE** -- the F4 build gate.

## packages/dsh-daily-work/package.json

`@deepseek-ai/dsh-attachment` added to `peerDependencies` and `devDependencies`
(it is now a production import, and the DEP-03 scan correctly flagged it as
undeclared). No version range change to any existing entry.

## Evidence regenerated

`qualification/results/M-DEP-SEC-UPG/declared-vs-imported.txt` -- regenerated by
running the DEP-03 test (see the report for the exact command). R4 regenerated the
same file from its own worktree; the coordinator has recorded this as `G-SEAM-66`
and will regenerate once on the integrated tree.
