# R9 verification overlay: the R9-owned copy of the deliverable-surface probe.
# Adds NO tool row; only the probe itself, so the counts it reports come from the
# profile's own composition.
- insert:
    - id: r9-deliverable-surface
      name: 'D:/DSH/work/dsh-native-daily/qualification/results/R9-delivery/r9-surface-probe.mjs'
