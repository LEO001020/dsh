=== tsc: the package (production sources) ===
command: tsc -p tsconfig.json --noEmit
date:    2026-09-19T11:59:11Z

tsc_exit=0

=== tsc: the test file and the A10 adapter ===
note: tsconfig.json excludes src/**/*.test.ts, and the adapter sits at the
      package root so a launcher subprocess can resolve @deepseek-ai/* from
      it. Both are checked separately against the same option set.

tsc_test_exit=0
