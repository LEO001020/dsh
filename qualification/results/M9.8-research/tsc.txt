# The package tsconfig EXCLUDES src/**/*.test.ts, so tsc -p tsconfig.json --noEmit
# exits 0 with or without this file. That is a FALSE PASS for a test artifact, so
# the second command below is the one that actually covers it: the same strict
# flags, the package's own exclude cleared, config kept OUTSIDE the repo.

$ cd /d/DSH/work/dsh-native-daily/packages/dsh-daily-work
$ export PATH="/d/DSH/tools/bin:/d/DSH/src/dsh-src/node_modules/.bin:$PATH"
$ tsc -p tsconfig.json --noEmit

package_tsc_exit=0

$ tsc -p /tmp/tsconfig.research.json   # files: src/research.test.ts, src/web-search.ts

test_tsc_exit=0
