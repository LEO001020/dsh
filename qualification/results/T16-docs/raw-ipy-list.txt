src/lifecycle.test.ts > IPY-04: the Session owns kernel identity, not the Agent activation > two Agent objects on one Session reach the same kernel PROCESS, and two Sessions do not
src/lifecycle.test.ts > IPY-05: an activation ending does not destroy a reusable kernel > ending an activation leaves the kernel alive, unchanged, and still bound to the Session
src/lifecycle.test.ts > IPY-05: an activation ending does not destroy a reusable kernel > evict is the explicit way to end a kernel, and it is host-only
src/lifecycle.test.ts > IPY-09: raw and rich output ordering is defined > stdout order and display order are each preserved, and they are separate sequences
src/lifecycle.test.ts > IPY-09: raw and rich output ordering is defined > stderr is its own ordered sequence, separate from stdout
src/lifecycle.test.ts > IPY-12: no automatic cell replay > a cell that kills its own kernel is not re-run in the replacement
src/lifecycle.test.ts > IPY-13: the broker is owned by the DSH subprocess seam > the classifier has teeth: it reports nothing when nothing of ours runs, and finds a started kernel
src/lifecycle.test.ts > IPY-13: the broker is owned by the DSH subprocess seam > the broker runs under the subprocess-local runner, not a bare spawn
src/lifecycle.test.ts > IPY-14: host-side loss is reconciled, and crash orphans are observed > a broker killed out from under the host is reported, and its kernel is not left stranded
src/lifecycle.test.ts > IPY-15: the kernel working directory is the Session's project root > os.getcwd() inside the kernel is the Session header cwd, per Session
src/lifecycle.test.ts > IPY-15: the kernel working directory is the Session's project root > a Session with no declared cwd falls back to the host root, never to an arbitrary directory
src/cleanup.test.ts > process hygiene > the classifier reports nothing of ours when nothing of ours is running
src/cleanup.test.ts > process hygiene > a started kernel is attributed to this package, so the classifier has teeth
src/cleanup.test.ts > process hygiene > shutdown leaves none of this package's processes running
src/cleanup.test.ts > process hygiene > two sequential kernels do not accumulate this package's processes
src/cleanup.test.ts > process hygiene > a kernel killed out from under the host is still cleaned up by shutdown
src/requirements.test.ts > requirement 1: a real IPython shell, not a CPython fake > get_ipython() is a ZMQInteractiveShell and a magic executes
src/requirements.test.ts > requirement 1: a real IPython shell, not a CPython fake > a second magic that inspects the shell state also runs
src/requirements.test.ts > requirement 2: kernel transport is not plaintext > the connection file carries curve keys and the plaintext warning is absent
src/requirements.test.ts > requirement 2: kernel transport is not plaintext > IPC is not available on this platform, and the start path does not pretend otherwise
src/requirements.test.ts > requirement 3: the namespace persists across cells > a DataFrame and a function built in cell 1 are usable in cell 2
src/requirements.test.ts > requirement 3: the namespace persists across cells > a mutation in cell 2 is visible in cell 3, so the object is genuinely shared
src/requirements.test.ts > requirement 4: top-level await works without a wrapper > a bare await at cell top level returns a value
src/requirements.test.ts > requirement 4: top-level await works without a wrapper > await works on a coroutine defined and called in the same cell
src/requirements.test.ts > requirement 4: top-level await works without a wrapper > a coroutine created in one cell can be awaited in the next
src/requirements.test.ts > requirement 5: an exception does not roll back the namespace > an assignment before a raise survives, and the error is reported clearly
src/requirements.test.ts > requirement 5: an exception does not roll back the namespace > an assignment made BEFORE the failing statement survives even when a later one does not
src/requirements.test.ts > requirement 6: stdin is disabled and fails fast > input() fails explainably instead of hanging
src/requirements.test.ts > requirement 6: stdin is disabled and fails fast > getpass fails the same way, and the kernel is still usable afterwards
src/requirements.test.ts > requirement 7: only the matching reply and idle complete a cell > a foreign shell frame during a cell does not end it
src/requirements.test.ts > requirement 7: only the matching reply and idle complete a cell > a stray reply left by wait_for_ready does not satisfy the first cell
src/requirements.test.ts > requirement 9: late output is classified separately, never attached to the next cell > output written after the cell went idle is classified late, not folded into the cell
src/requirements.test.ts > requirement 9: late output is classified separately, never attached to the next cell > output stamped with the live cell id but arriving after its idle is still classified late
src/requirements.test.ts > requirement 12: one model-facing tool named ipython > the tool declares exactly one required parameter, code
src/requirements.test.ts > requirement 12: one model-facing tool named ipython > no lifecycle tool name is exported or registered anywhere in the package
src/faults.test.ts > requirement 8: interrupt, then reuse the kernel > a CPU loop is interrupted, KeyboardInterrupt is reported, and the kernel is reusable
src/faults.test.ts > requirement 8: interrupt, then reuse the kernel > an interrupt of an await-suspended cell reports unknown and resets, never a false success
src/faults.test.ts > requirement 10: a flooding cell cannot OOM the host > hundreds of MB of stdout is capped, reported truncated, and spilled
src/faults.test.ts > requirement 10: a flooding cell cannot OOM the host > a cell under the cap reports no truncation, so the flag is not always-on
src/faults.test.ts > requirement 10: a flooding cell cannot OOM the host > a giant display payload is bounded too, not just stdout
src/faults.test.ts > requirement 11: kernel death changes the generation > killing the kernel reports a NEW epoch and a lost-state notice
src/faults.test.ts > requirement 11: kernel death changes the generation > after the death is reported, the replacement kernel is usable and empty
src/service.test.ts > ctx.ipython: the kernel registry > the service registers as ctx.ipython through the plugin path
src/service.test.ts > ctx.ipython: the kernel registry > two Sessions get two namespaces, and neither sees the other
src/service.test.ts > ctx.ipython: the kernel registry > a NEW Agent object on the SAME Session reaches the SAME kernel
src/service.test.ts > ctx.ipython: the kernel registry > a changed execution world is refused rather than served by the old kernel
src/service.test.ts > ctx.ipython: the kernel registry > a second service registration on one context is refused, preventing split-brain
src/service.test.ts > ctx.ipython: the kernel registry > evict removes the kernel and reports whether one existed
src/service.test.ts > ctx.ipython: the kernel registry > currentEpoch does not start a kernel, so a status read has no side effect
src/service.test.ts > ctx.ipython: the kernel registry > close shuts down every kernel it owns
src/service.test.ts > ctx.ipython: the kernel registry > a cancelled cell is refused before it is sent, not abandoned mid-flight
src/smoke.test.ts > broker transport > starts a kernel with curve keys and no plaintext warning
src/smoke.test.ts > broker transport > runs one cell and reports ok
src/protocol.test.ts > bounded framing > round-trips a message
src/protocol.test.ts > bounded framing > reassembles a frame split across arbitrary chunk boundaries
src/protocol.test.ts > bounded framing > decodes several frames delivered in one chunk
src/protocol.test.ts > bounded framing > rejects an oversized frame on the DECLARED length, without buffering it
src/protocol.test.ts > bounded framing > refuses to encode a value that exceeds the limit
src/protocol.test.ts > bounded framing > stops permanently after a consumer failure instead of misaligning
src/protocol.test.ts > bounded framing > rejects a payload that is not valid JSON
src/protocol.test.ts > broker message shape > accepts a well-formed reply and preserves the id
src/protocol.test.ts > broker message shape > requires a non-empty id, so a reply cannot be matched to nothing
src/protocol.test.ts > broker message shape > accepts a late_output event and defaults missing fields rather than throwing
src/protocol.test.ts > broker message shape > rejects an event with no epoch, because it cannot be attributed to a generation
src/protocol.test.ts > broker message shape > rejects an unknown type instead of guessing
