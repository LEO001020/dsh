# FABRICATED. No byte of this file came from a real credential store.
# The NAME is credential-shaped on purpose: it is what the scrub heuristic
# matches on, and the value is inert so a leak proves the leak and nothing else.
CANARY_FAKE_API_KEY=CANARY-FAKE-API-KEY-VALUE-0000-not-real
CANARY_FAKE_PASSWORD=CANARY-FAKE-PASSWORD-0000-not-real
CANARY_FAKE_TOKEN=CANARY-FAKE-TOKEN-0000-not-real
